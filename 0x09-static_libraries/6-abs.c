#include "main.h"

/**
 * _abs - function returns absolute value of input
 * @n: the input  to be checked
 * Return: return n if n is > 0 and 0 if n < 0
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
