#include "main.h"

/**
 * _isupper - the function that check if parameter is caps or not
 * @c: the parameter that ic to be ckecked
 * Return: returns a i if uppercase and 0 if not
 */

int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (0);
	}
	return (0);
}
