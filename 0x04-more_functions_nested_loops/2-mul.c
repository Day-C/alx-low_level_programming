#include "main.h"

/**
 * mul - mul is the function that multiplies two numbers
 * @a: a is the first parameter for the function
 * @b: b is the second parameter for the function.
 * Return: return the result from multiplication
 */

int mul(int a, int b)
{
	int m;

	m = a * b;
	return (m);
}
