#include "main.h"
/**
 * add - adds two numbers
 * @f: the first parameter to the function
 * @s: the second parameter to the function
 * Return: returs the answe when successful
 */
int add(int f, int s)
{
	int sum;

	sum = (f + s);
	return (sum);
}
